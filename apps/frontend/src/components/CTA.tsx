import { motion } from "motion/react";

export const CTA = () => {
    return (
        <motion.div className="sticky bottom-0 left-0 right-0 bg-brand text-black font-bold uppercase tracking-wider text-center py-4 z-50">
        Are you looking to book new events at your venue?
        </motion.div>
    );
}